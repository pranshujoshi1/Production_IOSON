/**
 * Test setup file — runs before every test file.
 * Imports jest-dom matchers so you can use:
 *   expect(element).toBeInTheDocument()
 *   expect(button).toBeDisabled()
 *   etc.
 */
import '@testing-library/jest-dom';
